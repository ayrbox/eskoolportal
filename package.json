{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "workspaces": ["packages/*"],
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start": "yarn workspace next run start",
    "heroku-postbuild": "yarn workspace next run build"
  }
}

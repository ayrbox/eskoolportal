describe('Student Repository <studentRepository.ts>', () => {
  describe('findById', () => {
    // it('should return student');
    // it('should return `null` if  not found');
  });

  describe('findByName', () => {
    // it('should return student detail on valid name');
    // it('should return null if not found');
  });

  describe('create', () => {
    //   it('should id of new student record');
    //   it('should throw if unable to save student');
  });

  describe('updateStudent', () => {
    //   it('should update student with new detail')
    //   it('should throw error on failed update')
  });

  describe('findByClass', () => {
    //     it('should return list of students')
    //     it('should return empty array if not found')
  });

  describe('Search', () => {
    it('should return list of studnes', () => {
      expect(200).toBe(200);
    });

    it('should return empty array', () => {
      expect(200).toBe(200);
    });
  });
});

{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "start": "yarn workspace next run start",
    "dev": "yarn workspace next run develop",
    "dev:api": "yarn workspace @eskoolportal/api run dev",
    "heroku-postbuild": "yarn workspace next run build"
  }
}
